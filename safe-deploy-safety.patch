*** Begin Patch
*** Update File: scripts/get_on_git_and_deploy.sh
@@
 set -euo pipefail
+# SAFETY: do not push to remote unless CONFIRM_PUSH=yes
+CONFIRM_PUSH="${CONFIRM_PUSH:-no}"
+if [ "${CONFIRM_PUSH}" != "yes" ]; then
+  echo "CONFIRM_PUSH is not 'yes' — script will not push to remote. To enable pushing set CONFIRM_PUSH=yes"
+  SKIP_PUSH=1
+fi
+
+# WARNING: Avoid passing GITHUB_TOKEN directly in remote URLs. Prefer SSH remotes or gh CLI to avoid token leakage.
*** End Patch
*** Begin Patch
*** Update File: scripts/white_label_automation.sh
@@
-set -euo pipefail
+# SAFETY: default to dry-run unless explicitly enabled via --run or RUN_MODE=run
+DRY_RUN=1
+for arg in "$@"; do
+  case "$arg" in
+    --run|--execute) DRY_RUN=0 ;;
+  esac
+done
+if [ "$DRY_RUN" = 1 ]; then
+  echo "WHITE-LABEL: running in dry-run mode. To perform real deploy pass --run or set RUN_MODE=run"
+fi
@@
-SSH_OPTS=(-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ConnectTimeout=10)
+# By default we keep host key checking enabled. Use --run plus --allow-unsafe-hostkey to bypass (discouraged).
+SSH_OPTS=(-o ConnectTimeout=10)
*** End Patch
*** Begin Patch
*** Update File: scripts/create_client_from_template.sh
@@
-deploy:
-  host: "${DEPLOY_HOST}"
-  path: "/var/www/${CLIENT_Slug:-$CLIENT_SLUG}"
+deploy:
+  host: "${DEPLOY_HOST}"
+  path: "/var/www/${CLIENT_SLUG}"
*** End Patch
